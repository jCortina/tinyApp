<!DOCTYPE html>
<html lang="en">
<head>
  <title>Header</title>
 </head>
 <body>
   <% if (userID) { %>
    <form method="POST", action="/logout">
      <h3>
        <p>Logged in as:  <span><%= userID %></span><span>   |  email: <%= user.email %></span></p>
        <input type="submit" id="logout" value="LOGOUT">
      </h3>
    </form>
    <% } else { %>
    <div>   
      <h2>You are not Loggen In</h2>
      <h3>
        <a href="/register">Register</a><br>
        <a href="/login">Login</a>
      </h3>
    </div>
    <% } %>
 </body>
 </html>
