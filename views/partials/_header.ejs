<DOCTYPE html>
<html lang="en">
<head>
  <title>Header</title>
 </head>
 <body>
   <% if (locals.user.id) { %>
    <form method="POST", action="/logout">
      <h4>
        <label for="logout"><%= user.id %></label>
        <input type="submit" id="logout" value="LOGOUT">
      </h4>
    </form>
    <% } else { %>
    <h3>
      <a href="/register">Register</a><br>
      <a href="/login">Login</a>
    </h3>
    <% } %>
 </body>
 </html>
 